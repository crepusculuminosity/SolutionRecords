#include <bits/stdc++.h>
using namespace std;
constexpr int maxn=(1<<15)+10;
int a[20];



int main() {
  int n,x,y; 
  while(~scanf("%d", &n)&&n) {
    scanf("%d%d", &x,&y);
    for(int i=1;i<=n;i++) scanf("%d", a+i);
  }
}