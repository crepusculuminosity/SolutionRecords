#include <bits/stdc++.h>
using namespace std;
map<char, bool> mp;
stack<bool> s;

int main() {
  freopen("data.in", "r", stdin);
  int n;
  cin >> n;
  for (int i = 1; i <= n; i++) {
    char c;
    cin >> c;
  }
  getchar();
  string str;
  getline(cin, str);
  cout << str;
  return 0;
}