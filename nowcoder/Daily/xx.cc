#include <bits/stdc++.h>
using namespace std;
map<string, string> m;

int main() {
  freopen("data.in", "r", stdin);
  ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
  int n;
  cin >> n;
  while (n--) {
    string b;
    getline(cin, b);

    cout << b << endl;
  }
  return 0;
}