#include <bits/stdc++.h>
using namespace std;
int n, m, s;
int ans;
int a;

void dfs(int v) {
  if (v < 0 || v >)
    for (int i = 0; i < 2; i++) {
      if (v >)
    }
}

int main() {
  while (~scanf("%d%d%d", &s, &n, &m) && s) {
    /* code */
    if (s % 2)
      printf("NO\n");
    else {
    }
  }
}