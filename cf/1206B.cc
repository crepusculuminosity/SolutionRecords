#include <bits/stdc++.h>
using namespace std;
constexpr int maxn = 1e5 + 10;

int main() {
  int n;
  cin >> n;
  int ans = 0;
  for (int i = 1; i <= n; i++) {
    int x;
    cin >> x;
    if (x == 0)
    // ans+=min(abs(-1-x),abs(1-x));
  }
}
