#include <bits/stdc++.h>
using namespace std;
const int maxn = 1010;
vector<int> G[maxn];
int in[maxn], d[maxn];
int n, m, r;

int main() {
  cin >> n >> m >> r;
  for (int i = 1; i <= m; i++) {
    int a, b, c;
    cin >> a >> b >> c;
  }
}