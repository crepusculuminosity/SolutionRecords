#include <bits/stdc++.h>
using namespace std;
int a[50][50];
int n,m;
const int INF = 0x3f3f3f3f;

int main() {
  freopen("data.in", "r", stdin);
  freopen("data.out", "w", stdout);
  while (cin >> n && n) {
    memset(a, 0x3f, sizeof(a));
    for(int i=0;i<)
    
  }
  return 0;
}