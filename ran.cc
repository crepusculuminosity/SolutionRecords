#include <bits/stdc++.h>
using namespace std;


int main() {
 // freopen("data.in","r",stdin);
  freopen("data.out","w",stdout);
  int d[]={4,45,534,1,67,0,0,0,0,0};
  sort(d,d+5);
  for(int i=0;i<9;i++)
    cout<<d[i]<<'\n';
  return 0;
}